# Word 样式清理（word-style-cleaner）

清理 Word 文档中未使用的自定义样式的小工具：找出文档中没有引用的自定义样式并删除。默认产出一个新文件（原文件不动）；高级用户可开启"覆盖原文件"模式（需确认）。

## Language

**自定义样式（custom style）**：
Word 文档中非内置（non-builtin）的样式，是唯一有资格被清理的样式。
_Avoid_：用户样式、额外样式

**未使用的样式（unused style）**：
在文档任何位置都没有被引用的自定义样式。引用位置包括：正文段落、表格（含单元格段落）、字符 run、页眉/页脚、脚注/尾注、文本框。任一位置出现即算"使用中"。
_Avoid_：多余样式、垃圾样式（含义更宽，不等于"未使用"）

**内置样式（builtin style）**：
Word 自带的样式，无论是否使用都永不清理。
_Avoid_：默认样式

**覆盖模式（overwrite mode）**：
清理结果直接替换原文件的输出模式，默认关闭；开启需显式操作并确认（GUI 弹窗确认，CLI 显式 flag）。默认输出 _Q 副本。
_Avoid_：直接保存、原地清理
